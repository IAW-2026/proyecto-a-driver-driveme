/**
 * @deprecated Theme toggling has been removed. Dark mode is the only mode.
 * This file exists solely to prevent import errors during transition.
 * Remove all references to ThemeToggle from the codebase.
 */
export default function ThemeToggle() {
  return null;
}